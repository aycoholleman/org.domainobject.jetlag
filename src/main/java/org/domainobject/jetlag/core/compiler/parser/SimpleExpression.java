package org.domainobject.jetlag.core.compiler.parser;

import org.domainobject.jetlag.core.compiler.tokenizer.TokenType;

public class SimpleExpression extends AbstractExpression {

	// private Logical

	@Override
	public void parse() throws ParseException
	{
		// if (tokenizer.at().type() == TokenType.LPAREN) {
		// if (tokenizer.hasMoreTokens())
		// throw new EOFException(/* TODO */);
		// tokenizer.nextToken();
		// SimpleExpression nested = getExpression();
		// effectiveParser = nested.effectiveParser;
		// type = nested.type;
		// if (tokenizer.nextToken() == null)
		// throw new EOFException(/* TODO */);
		// if (tokenizer.at().type() != TokenType.RPAREN)
		// throw new ParseException(/* TODO */);
		// }
		// else {
		// AbstractExpression parser = startOtherParser(new LogicalExpression());
		// effectiveParser = parser.effectiveParser;
		// type = parser.type;
		// }
	}

}
